server:
  servlet:
    context-path: /item-storage/api/v${item.api.version}

item:
  api:
    version: 1

spring:
  boot:
    admin:
      client:
        enabled: false
  cloud:
    discovery:
      enabled: false

springdoc:
  api-docs:
    enabled: false

logging:
  level:
    root: ERROR